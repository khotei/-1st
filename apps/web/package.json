{
  "name": "@1st/web",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "tsc": "tsc --noEmit",
    "clean": "rm -rf node_modules && rm -rf .next && rm -rf build"
  },
  "dependencies": {
    "@1st/components": "workspace:*",
    "@1st/dash": "workspace:*",
    "@elysiajs/eden": "catalog:api",
    "elysia": "catalog:api",
    "client-only": "0.0.1",
    "server-only": "0.0.1",
    "next": "16.0.0-beta.0",
    "react": "catalog:client",
    "react-dom": "catalog:client"
  },
  "devDependencies": {
    "@1st/api": "workspace:*",
    "babel-plugin-react-compiler": "catalog:client",
    "typescript": "catalog:runtime",
    "@types/react": "catalog:client",
    "@types/react-dom": "catalog:client",
    "@types/node": "24.7.2",
    "bun-types": "catalog:runtime"
  }
}
